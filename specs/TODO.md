# TODO

## Specification Gaps

- [x] Define module import/resolution semantics precisely.
- [x] Specify strictness/evaluation order of `let` bindings formally.
- [x] Define the exact set of "primitive types" vs "standard library types".
- [x] Specify error handling for `Effect` (using `Effect E A`).

## Tooling

- [ ] Language Server Protocol (LSP) implementation.
- [ ] Syntax highlighter (Treesitter grammar).
- [ ] Formatter (`aivi-fmt`).

## Implementation

- [ ] Prototype Compiler (to WASM?).
- [ ] Standard Library (collections, strings, IO).
- [ ] REPL.

## Documentation

- [ ] "AIVI by Example" tutorial series.
- [ ] Cookbook for common data pipeline tasks.
