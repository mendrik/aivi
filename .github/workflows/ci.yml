name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      aivi: ${{ steps.filter.outputs.aivi }}
      vscode: ${{ steps.filter.outputs.vscode }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            aivi:
              - "Cargo.toml"
              - "Cargo.lock"
              - "rust-toolchain.toml"
              - "crates/**"
              - "examples/**"
              - ".github/workflows/ci.yml"
            vscode:
              - "vscode/**"
              - "crates/aivi_lsp/**"
              - ".github/workflows/ci.yml"
            docs:
              - "specs/**"
              - ".github/workflows/ci.yml"

  aivi:
    name: AIVI (fmt, clippy, test)
    needs: changes
    if: needs.changes.outputs.aivi == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.90.0
          components: rustfmt, clippy
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Run clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Run tests
        run: cargo test --workspace

  aivi_perf:
    name: AIVI (perf smoke)
    needs: changes
    if: needs.changes.outputs.aivi == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.90.0
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
      - name: Perf check (lenient)
        run: cargo run -p aivi --bin perf -- check --baseline crates/aivi/perf/baseline.json --max-multiplier 2.0

  aivi_fuzz:
    name: AIVI (fuzz smoke)
    needs: changes
    if: needs.changes.outputs.aivi == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust (nightly)
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: nightly
          components: llvm-tools-preview
      - name: Rust cache
        uses: Swatinem/rust-cache@v2
      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --locked
      - name: Fuzz parser (short)
        run: cargo +nightly fuzz run parser -- -max_total_time=30
      - name: Fuzz frontend (short)
        run: cargo +nightly fuzz run frontend -- -max_total_time=30
      - name: Fuzz runtime (short)
        run: cargo +nightly fuzz run runtime -- -max_total_time=30

  docs:
    name: Docs (build)
    needs: changes
    if: needs.changes.outputs.docs == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: specs/package.json
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: specs/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        working-directory: specs
      - name: Build docs
        run: pnpm run docs:build
        working-directory: specs
        env:
          BASE_PATH: /

  vscode:
    name: VS Code Extension (build + package)
    needs: changes
    if: needs.changes.outputs.vscode == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.90.0
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: vscode/pnpm-lock.yaml
      - name: Install Node dependencies
        run: pnpm install --frozen-lockfile
        working-directory: vscode
      - name: Build LSP (release)
        run: cargo build -p aivi-lsp --release
      - name: Copy LSP into extension
        run: |
          set -euo pipefail
          exe="aivi-lsp"
          if [ "${{ runner.os }}" = "Windows" ]; then exe="aivi-lsp.exe"; fi
          mkdir -p vscode/bin
          cp "target/release/${exe}" "vscode/bin/${exe}"
          chmod +x "vscode/bin/${exe}" || true
      - name: Generate syntaxes
        run: cargo run -p aivi --bin gen_vscode_syntax -- "vscode/syntaxes"
      - name: Compile extension
        run: pnpm run compile
        working-directory: vscode
      - name: Package VSIX
        run: pnpm exec vsce package --no-dependencies --out aivi-vscode.vsix
        working-directory: vscode
      - name: Upload VSIX
        uses: actions/upload-artifact@v4
        with:
          name: aivi-vscode.vsix
          path: vscode/aivi-vscode.vsix
