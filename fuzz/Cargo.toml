[package]
name = "aivi-fuzz"
version = "0.0.0"
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"

# Fuzz the public toolchain surface area.
aivi = { path = "../crates/aivi" }

[profile.release]
debug = 1

