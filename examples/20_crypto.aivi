module examples.stdlib_crypto
export main

use aivi.console (print)
use aivi.crypto

main : Effect Text Unit
main = effect {
  hash = sha256 "aivi"
  _ <- print "sha256: {hash}"

  uuid <- randomUuid
  _ <- print "uuid: {uuid}"

  bytes <- randomBytes 16
  _ <- print "bytes: {bytes}"

  pure Unit
