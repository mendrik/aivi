module examples.compiler.literals = {
  export ints, floats, texts, suffixes, instant

  ints = [0, 1, 42, -7]
  floats = [0.0, 3.14, -2.5]
  texts = ["plain", "Count: {1 + 2}", "user: { { name: \"A\" }.name }"]

  suffixes = [10px, 100%, 30s, 1min, 3.14dec, 42n]
  instant = 2024-05-21T12:00:00Z
}
