module examples.compiler.functions = {
  export inc, add, compose, pipeline, getName, pairSum

  inc = _ + 1
  add = x y => x + y
  compose = f g => x => f (g x)

  pipeline = x => x |> inc |> (_ * 2)
  getName = .name
  pairSum = (a, b) => a + b
}
